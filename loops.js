function forLoop(array) {
  var results ='';
  var count = 0;
  for (let i = 0; i < 25; i++) {
   if (i === 1){
    results.push('I am ' + array[i] + ' strange loop.');
     
   } else {
     results.push()
   }
   }
   
  }
}
