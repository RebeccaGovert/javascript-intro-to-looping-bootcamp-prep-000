function forLoop(array) {
  var resultsArray= [];
  var count = 0;
  var myOutputLine = '';
  for (let i=0; i<array.length; i++) {
    if (i === 1) {
      myOutputLine += `I am ${i} strange loop
    }
  }
}